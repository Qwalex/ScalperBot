# --- Настройки подключения к API Bybit ---
BYBIT_KEY=NiKyhxzwkVUvPsQoGK         # API-ключ для доступа к аккаунту Bybit
BYBIT_SECRET=EOUhMjlTw5ZVtfBBfn7vGWRBezOEp57IFnST # Секретный ключ для API Bybit
BYBIT_TESTNET=false                  # Использовать тестовую сеть Bybit (true - тестовая, false - боевая)

# --- Торговые параметры ---
CATEGORY=linear                     # Категория инструмента: linear (USDT-маржинальные), inverse, spot, option
SYMBOL=DOGEUSDT                     # Торговый инструмент (пара), например DOGEUSDT
SLIPPAGE_TOL_TYPE=TickSize          # Тип допуска проскальзывания: TickSize (в тиках), Percent (в процентах)
SLIPPAGE_TOL=10                     # Допустимое проскальзывание (например, 10 тиков или 10 процентов, в зависимости от SLIPPAGE_TOL_TYPE)

# --- Режим работы и логирование ---
DRY_RUN=false                       # Режим тестирования: true - сделки не отправляются на биржу, false - реальные сделки

# базовые
LEVERAGE=1
ORDER_QTY=27
TIME_IN_FORCE=PostOnly
CANCEL_AFTER_MS=6000

# спред/отступ
MIN_SPREAD_TICKS=6               # порог исходного спреда
MIN_NET_SPREAD_TICKS=2              # чистый спред после отступов (минимум)
EDGE_TICKS=2                        # базовый отступ от края (используем как fallback)

# динамический EDGE (от волатильности, в тиках)
DYN_EDGE_BASE=2
DYN_EDGE_MAX=6
DYN_EDGE_VOL_SCALE=4                # чем меньше — тем сильнее растёт EDGE при воле

# фильтры микро-структуры/импульса
SIZE_IMBALANCE_TOL=0.25             # 0.2..0.4 разумно
TRADE_AGG_WINDOW_MS=800
AGGRESSOR_BIAS_TOL=0.3              # >0.3 режем противоположную сторону

# частота
MAX_OPEN_ORDERS=1
MAX_ORDERS_PER_MINUTE=12
GLOBAL_COOLDOWN_MS=0

# логирование
LOG_LEVEL=debug
LOG_PRETTY=true